require:
  - rubocop-rails

AllCops:
  Include:
    - db/migrate/*

Rails/SchemaComment:
  Enabled: true

# Enable this cop when rubocop-rails version >= 2.29.0 is used on all projects
# Rails/StrongParametersExpect:
#  Enabled: false
